export interface Translation {
  es: Detail;
  en: Detail;
}

export interface Detail {
  headerTitle:    string;
  headerSubtitle: string;
  headerLight:    string;
  headerDark:     string;
  buttonLogin:    string;
  buttonLogout:   string;
  mainWelcome:    string;
  mainHello:      string;
  mainContent:    string;
  footerTitle:    string;
}
